"use strict";
// satu variable hanya bisa satu type data 
describe('tipe Type', function () {
    it('should must declare', function () {
        const name = "Roid Robih";
        const balance = 100;
        const isVip = true;
        console.log(name);
        console.log(balance);
        console.log(isVip);
    });
});
