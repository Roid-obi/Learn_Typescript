// sama seperti js

describe('Ternary Operator', function () {
    it('should support Ternary', function () {
        const value = 80;
        const say = value >= 75 ? "Suramat, Kamu Sukses" : "NT Bang"; 
        
        console.info(say);
    });
});